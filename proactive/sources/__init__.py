"""Per-source adapters that normalize native triggers into ``ProactiveEvent``.

Email lives in ``proactive.sources.email``. Other sources (attention fire,
attention offer, calendar, pattern) come online in later phases.
"""
